package com.stocktcg.roots;

import javafx.scene.Parent;
import javafx.scene.Scene;
public class RootSwap {
    public static void swapeRoot(Scene scene, Parent newRoot){ 
        scene.setRoot(newRoot);
    } 
}
